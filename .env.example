# Environment Variables — copy to .env for local development
# Never commit .env to source control

# Azure AI Foundry — replace placeholders with your actual resource names
AZURE_ENDPOINT=https://<your-resource>.services.ai.azure.com/
AZURE_AI_PROJECT=https://<your-resource>.services.ai.azure.com/api/projects/<your-project>
AZURE_JUDGE_DEPLOYMENT=gpt-4o
AZURE_API_VERSION=2025-05-01-preview

# For local dev: run `az login` and DefaultAzureCredential will pick up your session.
# For CI: set AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID
# as GitHub Actions secrets.
